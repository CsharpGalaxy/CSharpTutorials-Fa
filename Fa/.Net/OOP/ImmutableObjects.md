

# 📚 Immutable Objects در اصول طراحی نرم‌افزار با C#

> این مستند برای استفاده در مخزن GitHub شما با هدف آموزش برنامه‌نویسی شیءگرا (OOP) در C# نوشته شده و مفاهیم Immutable Objects را از دیدگاه اصول طراحی (Design Principles) با رویکردی ساده و مستندشده ارائه می‌دهد.

---

## 📖 فهرست مطالب

1. [مقدمه: Immutable یعنی چه؟](#1-مقدمه-immutable-یعنی-چه)
2. [چرا Immutable Objects مهم هستند؟](#2-چرا-immutable-objects-مهم-هستند)
3. [ویژگی‌های یک Immutable Object](#3-ویژگی‌های-یک-immutable-object)
4. [پیاده‌سازی Immutable Objects در C#](#4-پیاده‌سازی-immutable-objects-در-c)
   - [روش سنتی (قبل از C# 9)](#روش-سنتی-قبل-از-c-9)
   - [استفاده از `record` در C# 9+](#استفاده-از-record-در-c-9)
5. [Immutable Objects و اصول طراحی نرم‌افزار](#5-immutable-objects-و-اصول-طراحی-نرم‌افزار)
6. [نکات عملی و بهترین روش‌ها (Best Practices)](#6-نکات-عملی-و-بهترین-روش‌ها-best-practices)
7. [چالش‌ها و محدودیت‌ها](#7-چالش‌ها-و-محدودیت‌ها)
8. [جمع‌بندی](#8-جمع‌بندی)
9. [منابع معتبر](#9-منابع-معتبر)

---

## 1. مقدمه: Immutable یعنی چه؟

کلمه **Immutable** به معنای «غیرقابل تغییر» است. یک **Immutable Object** شی‌ای است که پس از ساخته شدن، **هیچ‌یک از حالت‌های (state) آن قابل تغییر نیست**. هر عملی که «به نظر» تغییر ایجاد می‌کند، در واقع یک شیء جدید با حالت اصلاح‌شده تولید می‌کند.

در مقابل، **Mutable Objects** اجازه تغییر حالت پس از ساخت را می‌دهند (مثل معمولی‌ترین کلاس‌های C#).

---

## 2. چرا Immutable Objects مهم هستند؟

Immutable Objects در طراحی نرم‌افزار مزایای مهمی دارند:

- **Thread-safe بودن**: چون هیچ تردی نمی‌تواند شیء را تغییر دهد، نیازی به قفل (lock) نیست.
- **Predictability**: رفتار شیء قابل پیش‌بینی است — همیشه همان حالتی را دارد که در ساخت ایجاد شده.
- **Debugging ساده‌تر**: چون حالت شیء ثابت است، ردیابی باگ‌ها آسان‌تر می‌شود.
- **الگوهای Functional Programming**: Immutable Objects پایه‌ای برای الگوهای تابعی هستند (مثل `Select`, `Where` در LINQ).
- **کاهش Side Effects**: تغییرات غیرمنتظره در سایر بخش‌های برنامه کمتر رخ می‌دهد.

---

## 3. ویژگی‌های یک Immutable Object

یک شیء در C# را Immutable می‌نامیم اگر:

- تمام فیلدها (`fields`) یا پراپرتی‌ها (`properties`) **فقط خواندنی** (read-only) باشند.
- مقادیر فقط در **زمان ساخت (constructor)** تنظیم شوند.
- هیچ متدی وجود نداشته باشد که حالت داخلی شیء را تغییر دهد.
- اگر از انواع مرجع (reference types) استفاده شده، آن‌ها نیز باید Immutable باشند (یا دفاعی کپی شوند).

> ⚠️ نکته مهم: Immutable ≠ Immutable Interface! حتی اگر پراپرتی‌ها `public get` داشته باشند، اما `set` داخلی نداشته باشند، باز هم Immutable هستند.

---

## 4. پیاده‌سازی Immutable Objects در C#

### روش سنتی (قبل از C# 9)

```csharp
public class Person
{
    public string Name { get; }
    public int Age { get; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }
}
```

در این مثال:

- هیچ `set` وجود ندارد → پراپرتی‌ها فقط خواندنی هستند.
- مقادیر فقط در سازنده (Constructor) مقداردهی می‌شوند.

> نیازی به `readonly` برای پراپرتی‌ها نیست، چون بدون `set` به‌طور ضمنی فقط خواندنی هستند.

### استفاده از `record` در C# 9+

با معرفی `record` در C# 9، مایکروسافت پشتیبانی رسمی برای Immutable Types اضافه کرد:

```csharp
public record Person(string Name, int Age);
```

این کد به‌صورت خودکار:

- یک سازنده با پارامترهای `Name` و `Age` تولید می‌کند.
- پراپرتی‌های `init-only` یا `get-only` ایجاد می‌کند.
- متد `With()` برای ایجاد نسخه‌های اصلاح‌شده فراهم می‌کند:

```csharp
var person = new Person("Ali", 30);
var olderPerson = person with { Age = 31 }; // شیء جدید
```

> ✅ `record`ها به‌طور پیش‌فرض Immutable هستند (البته می‌توان `record class` یا `record struct` قابل تغییر هم تعریف کرد، اما استاندارد Immutable است).

---

## 5. Immutable Objects و اصول طراحی نرم‌افزار

Immutable Objects با چندین اصل طراحی هم‌راستا هستند:

### ✅ **Principle of Least Astonishment (PLA)**
کاربر انتظار ندارد که یک شیء بدون دستور تغییر کند → Immutable Objects این انتظار را برآورده می‌کنند.

### ✅ **Command-Query Separation (CQS)**
روش‌هایی که تغییر ایجاد می‌کنند (Commands) باید `void` باشند. در Immutable Objects، هر "تغییر" یک Query است (`With()`) که شیء جدید برمی‌گرداند → این اصل را حفظ می‌کند.

### ✅ **Single Responsibility Principle (SRP)**
شیء فقط مسئول نگهداری حالت خود است، نه تغییر آن.

### ✅ **Fail Fast**
اگر کدی سعی کند یک Immutable Object را تغییر دهد، یا کامپایل نمی‌شود یا استثنا می‌دهد → باگ زودتر شناسایی می‌شود.

---

## 6. نکات عملی و بهترین روش‌ها (Best Practices)

- از `record` برای انواع داده‌ای ساده (Data Transfer Objects, Value Objects) استفاده کنید.
- اگر از کلاس معمولی استفاده می‌کنید، تمام پراپرتی‌ها را بدون `set` یا با `init` (در C# 9+) تعریف کنید.
- برای کالکسیون‌ها (`List<T>`, `Dictionary<K,V>` و غیره)، از `ReadOnlyCollection<T>` یا `Immutable Collections` (از `System.Collections.Immutable`) استفاده کنید.
- هنگام استفاده از انواع مرجع (مثل لیست‌ها)، در سازنده یک **کپی دفاعی (defensive copy)** بگیرید:

```csharp
public class Team
{
    public IReadOnlyList<string> Members { get; }

    public Team(IEnumerable<string> members)
    {
        Members = members.ToList().AsReadOnly(); // defensive copy
    }
}
```

---

## 7. چالش‌ها و محدودیت‌ها

- **هزینه حافظه**: هر تغییر یک شیء جدید ایجاد می‌کند → ممکن است برای داده‌های بزرگ مشکل‌ساز شود.
- **عملکرد**: در حلقه‌های سنگین (مثل بروزرسانی مکرر یک شیء)، تخصیص حافظه زیاد می‌شود.
- **استفاده نادرست از کالکسیون‌ها**: اگر کالکسیون داخلی Mutable باشد، شیء به‌طور کامل Immutable نیست!

---

## 8. جمع‌بندی

Immutable Objects یکی از ابزارهای قدرتمند در طراحی نرم‌افزارهای قابل اعتماد، ایمن از نظر همزمانی (thread-safe)، و قابل نگهداری هستند. در C#، با استفاده از `record`ها و رعایت اصول ساده، پیاده‌سازی آن‌ها بسیار آسان است. این مفهوم هم با اصول OOP و هم با الگوهای Functional Programming هماهنگی دارد.

---

## 9. منابع معتبر

1. **Microsoft Learn – Records (C# reference)**  
   🔗 https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/record

2. **Microsoft Learn – Immutable Objects**  
   🔗 https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/classes-and-structs/how-to-implement-a-lightweight-class-with-auto-implemented-properties#immutable-types

3. **System.Collections.Immutable Documentation**  
   🔗 https://learn.microsoft.com/en-us/dotnet/api/system.collections.immutable

4. **"C# in Depth" – Jon Skeet** (Chapter on Immutability & Records)  
   🔗 https://manning.com/books/c-sharp-in-depth-fourth-edition

5. **Martin Fowler – Immutable Objects**  
   🔗 https://martinfowler.com/eaaDev/ImmutableValueObject.html

6. **Eric Lippert’s Blog – Immutability in C#**  
   🔗 https://ericlippert.com/category/immutability/

7. **Clean Code – Robert C. Martin** (Discussion on state & side effects)  
   🔗 https://www.oreilly.com/library/view/clean-code/9780136083238/
