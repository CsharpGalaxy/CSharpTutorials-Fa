

## 📚 فهرست مطالب

1. [مقدمه: این ویژگی چیست؟](#مقدمه-این-ویژگی-چیست)  
2. [چرا Top-Level Statements معرفی شد؟](#چرا-top-level-statements-معرفی-شد)  
3. [نحو (Syntax) و ساختار](#نحو-syntax-و-ساختار)  
4. [نکات مهم و محدودیت‌ها](#نکات-مهم-و-محدودیت‌ها)  
5. [استفاده در پروژه‌های واقعی: چه زمانی مناسب است؟](#استفاده-در-پروژه‌های-واقعی-چه-زمانی-مناسب-است)  
6. [نحوه غیرفعال‌کردن یا بازگشت به سبک سنتی](#نحوه-غیرفعال‌کردن-یا-بازگشت-به-سبک-سنتی)  
7. [تأثیر بر یادگیری OOP](#تأثیر-بر-یادگیری-oop)  
8. [جمع‌بندی](#جمع‌بندی)  
9. [منابع معتبر](#منابع-معتبر)

---

## مقدمه: این ویژگی چیست؟

**Top-Level Statements** (گزاره‌های سطح بالا) یک ویژگی جدید در **C# 9.0** (در سال 2020 معرفی شد) است که به توسعه‌دهندگان اجازه می‌دهد بدون نیاز به نوشتن کلاس‌ها، فضای نام (namespace)، یا متد `Main` صریح، کد خود را مستقیماً در بالای فایل بنویسند.

در سی‌شارپ قدیمی، یک برنامه ساده "Hello World" اینگونه بود:

```csharp
using System;

namespace MyProgram
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");
        }
    }
}
```

اما با Top-Level Statements، همان برنامه به این شکل ساده می‌شود:

```csharp
Console.WriteLine("Hello, World!");
```

> 🔍 **نکته مهم:** این ویژگی فقط در **یک فایل از پروژه** مجاز است — معمولاً همان فایل `Program.cs`.

---

## چرا Top-Level Statements معرفی شد؟

Microsoft با هدف **کاهش پیچیدگی اولیه** برای مبتدیان، به‌ویژه در آموزش و پروژه‌های ساده، این ویژگی را اضافه کرد:

- ✅ کاهش boilerplate code (کد تکراری و الزامی بدون منطق کسب‌وکار)
- ✅ جذب سریع‌تر مبتدیان به زبان C#
- ✅ شبیه‌تر شدن به زبان‌های اسکریپتی مانند Python برای نوشتن اسکریپت‌های سریع

اما این بدان معنا نیست که OOP یا معماری نرم‌افزاری نادیده گرفته شده است — بلکه **فقط برای نقطه ورود برنامه** ساده‌سازی شده است.

---

## نحو (Syntax) و ساختار

### ✅ ساده‌ترین حالت

```csharp
Console.WriteLine("Hi from top-level!");
```

### ✅ استفاده از آرگومان‌های خط فرمان

آرگومان‌های `Main` به‌صورت خودکار در یک آرایه با نام `args` در دسترس هستند:

```csharp
if (args.Length > 0)
{
    Console.WriteLine($"First argument: {args[0]}");
}
```

### ✅ تعریف توابع کمکی

می‌توانید توابع سراسری (global) در همان فایل تعریف کنید:

```csharp
int Add(int a, int b) => a + b;

Console.WriteLine(Add(2, 3)); // Output: 5
```

> ⚠️ این توابع به‌صورت **خودکار `static` و `private`** در یک کلاس پنهان تولید می‌شوند.

### ✅ بازگشتی از `Main`

اگر برنامه باید یک کد خروجی (`int`) برگرداند:

```csharp
if (args.Length == 0)
{
    return 1; // خطا
}

Console.WriteLine("OK");
return 0; // موفقیت
```

---

## نکات مهم و محدودیت‌ها

| مورد | توضیح |
|------|--------|
| **فقط یک فایل** | فقط **یک فایل** در کل پروژه می‌تواند Top-Level Statements داشته باشد. |
| **فقط در فایل ورودی** | معمولاً `Program.cs`؛ در فایل‌های دیگر نمی‌توان استفاده کرد. |
| **کلاس پنهان تولید می‌شود** | کامپایلر به‌صورت خودکار یک کلاس `Program` و متد `Main` ایجاد می‌کند. |
| **عدم سازگاری با ورژن‌های قدیمی** | نیاز به **.NET 5 یا بالاتر** و **C# 9 یا جدیدتر** دارد. |
| **عدم استفاده از کلمات کلیدی `public` / `class` در بالای فایل** | اگر کلاسی در بالای فایل تعریف کنید، Top-Level Statements دیگر فعال نیست. |

---

## استفاده در پروژه‌های واقعی: چه زمانی مناسب است؟

| نوع پروژه | توصیه |
|-----------|--------|
| **آموزش OOP / مفاهیم برنامه‌نویسی** | ⚠️ **با احتیاط** — چون پنهان‌کاری مفاهیم پایه (مثل `Main`) ممکن است یادگیری را دشوار کند |
| **کنسول‌اپلیکیشن‌های ساده (اسکریپت‌ها)** | ✅ **عالی** — برای ابزارهای CLI یا پروتوتایپ‌های سریع |
| **وب‌اپلیکیشن‌های ASP.NET Core** | ✅ **پیشنهاد شده** — از .NET 6 به بعد، الگوی پیش‌فرض `Program.cs` بدون کلاس است |
| **پروژه‌های بزرگ با معماری پیچیده** | ⚠️ **توصیه نمی‌شود** — بهتر است از ساختار کلاسیک استفاده کنید تا خوانایی و نگهداری حفظ شود |

> 📌 **نکته آموزشی:** اگر ریپوزیتوری شما برای **آموزش OOP** است، بهتر است **در مراحل اولیه از Top-Level Statements استفاده نکنید**، تا دانش‌آموزان بتوانند ساختار واقعی برنامه‌های C# را بفهمند.

---

## نحوه غیرفعال‌کردن یا بازگشت به سبک سنتی

اگر بخواهید دوباره از ساختار کلاسیک استفاده کنید:

1. فایل `Program.cs` را پاک کنید یا ویرایش کنید.
2. یک کلاس با `Main` ایجاد کنید:

```csharp
namespace MyApp;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("Back to classic C#!");
    }
}
```

> 💡 حتی می‌توانید در پروژه‌هایی که از Top-Level Statements استفاده می‌کنند، **فقط یک کلاس در بالای فایل قرار دهید** — و به‌محض انجام این کار، Top-Level Statements غیرفعال می‌شود.

---

## تأثیر بر یادگیری OOP

> 🔎 **سئوال مهم برای مربیان و مدرسین:** آیا این ویژگی یادگیری OOP را سخت‌تر می‌کند؟

**پاسخ:** بله، اگر در **مرحله اولیه آموزش** استفاده شود.

- مبتدیان ممکن است نفهمیوند که `Main` واقعاً چیست.
- مفهوم **نقطه ورود برنامه** (`entry point`) پنهان می‌شود.
- درک **namespaceها، کلاس‌ها، و static methods** دیرتر رخ می‌دهد.

✅ **پیشنهاد برای ریپوزیتوری شما:**
> ابتدا مفاهیم پایه (کلاس، اسم فضایی، Main) را با سبک سنتی آموزش دهید. سپس در پروژه‌های پیشرفته‌تر یا در بخش‌های مرتبط با .NET 6+، Top-Level Statements را به‌عنوان یک ویژگی مدرن معرفی کنید.

---

## جمع‌بندی

| مورد | نتیجه |
|------|--------|
| **آیا Top-Level Statements بد است؟** | ❌ خیر — برای کاربردهای خاص عالی است |
| **آیا در آموزش OOP مناسب است؟** | ⚠️ فقط پس از یادگیری مفاهیم پایه |
| **آیا جایگزین OOP می‌شود؟** | ❌ نه — تنها نقطه ورود برنامه را ساده می‌کند |
| **آیا در ASP.NET Core استفاده می‌شود؟** | ✅ بله — از .NET 6 پیش‌فرض است |

---

## منابع معتبر

1. **Microsoft Docs – Top-level statements (C# 9.0+)**  
   🔗 [https://learn.microsoft.com/en-us/dotnet/csharp/whats-new/tutorials/top-level-statements](https://learn.microsoft.com/en-us/dotnet/csharp/whats-new/tutorials/top-level-statements)

2. **C# Language Reference – Top-level statements**  
   🔗 [https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/proposals/csharp-9.0/top-level-statements](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/proposals/csharp-9.0/top-level-statements)

3. **.NET 6 Minimal Hosting Model (برای ASP.NET Core)**  
   🔗 [https://learn.microsoft.com/en-us/aspnet/core/migration/50-to-60?view=aspnetcore-8.0#new-hosting-model](https://learn.microsoft.com/en-us/aspnet/core/migration/50-to-60?view=aspnetcore-8.0#new-hosting-model)

4. **C# 9.0 and Beyond – Mads Torgersen (Lead Designer of C#)**  
   🔗 [https://devblogs.microsoft.com/dotnet/welcome-to-c-9-0/](https://devblogs.microsoft.com/dotnet/welcome-to-c-9-0/)

5. **Stack Overflow – When to use top-level statements?**  
   🔗 [https://stackoverflow.com/questions/64838042/when-to-use-top-level-statements](https://stackoverflow.com/questions/64838042/when-to-use-top-level-statements)



اگر نیاز به نسخه Markdown یا قالب‌بندی برای گیت‌هاب داشتید، خوشحال می‌شوم کمک کنم!
