
## فهرست مطالب

1. [مقدمه: چرا این اصل مهم است؟](#مقدمه-چرا-این-اصل-مهم-است)
2. [تعریف اصل Composition over Inheritance](#تعریف-اصل-composition-over-inheritance)
3. [ارث‌بری (Inheritance) چیست؟](#ارث‌بری-inheritance-چیست)
4. [ترکیب (Composition) چیست؟](#ترکیب-composition-چیست)
5. [مقایسه با مثال سی‌شارپ](#مقایسه-با-مثال-سی‌شارپ)
6. [مزایای استفاده از ترکیب](#مزایای-استفاده-از-ترکیب)
7. [معایب ارث‌بری در طراحی‌های پیچیده](#معایب-ارث‌بری-در-طراحی‌های-پیچیده)
8. [الگوهای طراحی مرتبط](#الگوهای-طراحی-مرتبط)
9. [نتیجه‌گیری](#نتیجه‌گیری)
10. [منابع معتبر](#منابع-معتبر)

---

## مقدمه: چرا این اصل مهم است؟

یکی از چالش‌های اصلی در برنامه‌نویسی شیءگرا (OOP)، طراحی کدهایی است که **انعطاف‌پذیر، قابل نگهداری و قابل تست** باشند.  
بسیاری از توسعه‌دهندگان مبتدی به‌سرعت از **ارث‌بری** (Inheritance) برای اشتراک‌گذاری رفتار بین کلاس‌ها استفاده می‌کنند. اما با رشد سیستم، این رویکرد می‌تواند منجر به **سلسله مراتب کلاس‌های پیچیده و سخت‌مدیریت** شود.

اصل **«ترکیب بر ارث‌بری»** یک جایگزین قدرتمند و انعطاف‌پذیر برای این مشکل ارائه می‌دهد.

---

## تعریف اصل Composition over Inheritance

> **"به‌جای اینکه رفتار را از طریق ارث‌بری به ارث ببرید، آن را از طریق ترکیب با اشیاء دیگر پیاده‌سازی کنید."**

به عبارت ساده‌تر:
- **ارث‌بری**: رابطه **"is-a"** (مثلاً `Duck` یک `Bird` است).
- **ترکیب**: رابطه **"has-a"** (مثلاً `Duck` یک `FlyBehavior` دارد).

**ترکیب** به شما اجازه می‌دهد بدون تغییر ساختار کلاس، رفتارهای مختلف را به یک شیء متصل کنید.

---

## ارث‌بری (Inheritance) چیست؟

در سی‌شارپ، زمانی از ارث‌بری استفاده می‌کنیم که یک کلاس از کلاس دیگری **مشتق** (Derive) شود:

```csharp
public class Bird
{
    public virtual void Fly()
    {
        Console.WriteLine("Bird is flying");
    }
}

public class Duck : Bird
{
    // Duck از Bird ارث‌بری می‌کند
}
```

**نکته مهم**: این روش زمانی مناسب است که رابطه **"is-a"** واقعاً وجود داشته باشد.

---

## ترکیب (Composition) چیست؟

ترکیب یعنی یک کلاس از **شیء دیگری به‌عنوان یک عضو داخلی** استفاده کند:

```csharp
public interface IFlyBehavior
{
    void Fly();
}

public class FlyWithWings : IFlyBehavior
{
    public void Fly() => Console.WriteLine("Flying with wings!");
}

public class Duck
{
    private readonly IFlyBehavior _flyBehavior;

    public Duck(IFlyBehavior flyBehavior)
    {
        _flyBehavior = flyBehavior;
    }

    public void PerformFly() => _flyBehavior.Fly();
}
```

در این مثال، `Duck` **رفتار پرواز** را از طریق **ترکیب** (Composition) به دست می‌آورد، نه از طریق ارث‌بری.

---

## مقایسه با مثال سی‌شارپ

### ❌ مشکل ارث‌بری: "مشکل اردک بدون بال"

فرض کنید برخی پرندگان مثل **پنگوئن** نمی‌توانند پرواز کنند. اگر از ارث‌بری استفاده کنیم:

```csharp
public class Penguin : Bird
{
    public override void Fly()
    {
        throw new NotSupportedException("Penguins can't fly!");
    }
}
```

این **نقض اصل Liskov Substitution Principle** (LSP) از SOLID است! چون نمی‌توانیم یک `Penguin` را جایگزین `Bird` کنیم بدون اینکه برنامه خراب شود.

### ✅ راه‌حل با ترکیب:

```csharp
var flyingDuck = new Duck(new FlyWithWings());
var penguin = new Duck(null); // یا یک FlyBehavior ساکت مثل NoFly

// یا بهتر:
public class NoFly : IFlyBehavior
{
    public void Fly() => Console.WriteLine("I can't fly :(");
}
```

حالا هر شیء می‌تواند **رفتار مناسب خودش** را داشته باشد، بدون نقض اصول OOP.

---

## مزایای استفاده از ترکیب

✅ **انعطاف‌پذیری بیشتر**: رفتارها را می‌توان در زمان اجرا تغییر داد.  
✅ **کاهش وابستگی**: کلاس‌ها به‌جای وابسته‌بودن به سلسله مراتب، به واسط‌ها (Interfaces) وابسته می‌شوند.  
✅ **سازگاری با SOLID**: به‌ویژه اصول **Dependency Inversion** و **Liskov Substitution**.  
✅ **قابلیت تست بهتر**: می‌توان رفتارها را با Mock جایگزین کرد.  
✅ **جلوگیری از "diamond problem"**: مشکل ارث‌بری چندگانه (در زبان‌هایی که پشتیبانی می‌کنند).

---

## معایب ارث‌بری در طراحی‌های پیچیده

- 🚫 **سلسله مراتب عمیق و شکننده**: تغییر در کلاس پایه، همه فرزندان را تحت تأثیر قرار می‌دهد.
- 🚫 **تعدیل رفتار سخت است**: اگر یک کلاس فرزند نیاز به رفتار متفاوتی داشته باشد، باید از `override` استفاده کنید که ممکن است منطق را شکسته کند.
- 🚫 **ارث‌بری چندگانه در سی‌شارپ پشتیبانی نمی‌شود** → محدودیت در طراحی.

> ⚠️ **نکته**: ارث‌بری **کاملاً بد نیست**! اما باید **فقط زمانی استفاده شود که رابطه is-a واقعی وجود داشته باشد** و **هیچ تضاد منطقی** در رفتار باشد.

---

## الگوهای طراحی مرتبط

ترکیب پایه‌ی بسیاری از **الگوهای طراحی** (Design Patterns) است:

- **Strategy Pattern**: همان مثال `FlyBehavior` در بالا!
- **Decorator Pattern**: افزودن رفتار به شیء بدون تغییر کد آن.
- **Adapter Pattern**: ترکیب یک کلاس با واسط دیگری برای سازگاری.

این الگوها به‌طور مستقیم از اصل **Composition over Inheritance** پیروی می‌کنند.

---

## نتیجه‌گیری

- **ارث‌بری** برای مدل‌سازی رابطه‌ی **"is-a"** مناسب است.
- **ترکیب** برای **افتراق رفتار** و **افزایش انعطاف‌پذیری** بهتر است.
- در سی‌شارپ، با استفاده از **واسط‌ها** (Interfaces) و **تزریق وابستگی** (Dependency Injection)، پیاده‌سازی ترکیب بسیار ساده و قدرتمند است.
- همیشه بپرسید: «آیا این رابطه واقعاً is-a است؟ یا بهتر است has-a باشد؟»

> ✨ **نکته نهایی**:  
> «Favor object composition over class inheritance.»  
> — *GoF, Design Patterns: Elements of Reusable Object-Oriented Software*

---

## منابع معتبر

1. **Gamma, E., Helm, R., Johnson, R., & Vlissides, J.** (1994).  
   *Design Patterns: Elements of Reusable Object-Oriented Software*.  
   Addison-Wesley.  
   → صفحه‌ی 20: «Favor object composition over class inheritance.»

2. **Microsoft Learn – C# Programming Guide**:  
   [Inheritance in C#](https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/tutorials/inheritance)  
   [Composition vs Inheritance](https://learn.microsoft.com/en-us/dotnet/standard/design-guidelines/inheritance-and-composition)

3. **Martin, R. C.** (2002).  
   *Agile Software Development, Principles, Patterns, and Practices*.  
   Prentice Hall.  
   → فصل درباره SOLID و Composition.

4. **SOLID Principles – Composition over Inheritance**:  
   [Refactoring.Guru – Composition over Inheritance](https://refactoring.guru/composition-over-inheritance)

5. **C# In Depth – Jon Skeet**:  
   (فصل‌های مربوط به OOP و روابط بین کلاس‌ها)

