
## فهرست مطالب

- [معرفی](#معرفی)
- [وراثت (Inheritance)](#وراثت-inheritance)
  - [تعریف و مفهوم](#تعریف-و-مفهوم)
  - [نحوه پیاده‌سازی در C#](#نحوه-پیاده‌سازی-در-c)
  - [کلیدواژه‌های مهم: `base`, `virtual`, `override`, `sealed`](#کلیدواژه‌های-مهم-base-virtual-override-sealed)
  - [وراثت و اصل جایگزینی لیسکوف (LSP)](#وراثت-و-اصل-جایگزینی-لیسکوف-lsp)
- [مخرب‌ها (Destructors)](#مخرب‌ها-destructors)
  - [تعریف و کاربرد](#تعریف-و-کاربرد)
  - [تفاوت با `IDisposable` و مدیریت منابع](#تفاوت-با-idisposable-و-مدیریت-منابع)
  - [چرخه زندگی شیء و Finalizer](#چرخه-زندگی-شیء-و-finalizer)
- [جمع‌بندی و اصول طراحی مرتبط](#جمع‌بندی-و-اصول-طراحی-مرتبط)
- [منابع معتبر](#منابع-معتبر)

---

## معرفی

در برنامه‌نویسی شیء‌گرا (OOP)، **وراثت** یکی از ستون‌های اصلی کپسوله‌سازی رفتارهای مشترک و کاهش تکرار کد است. از سوی دیگر، **مخرب‌ها** بخشی از چرخه زندگی شیء هستند که در آن منابع غیرمدیریت‌شده آزاد می‌شوند. در سی‌شارپ، به‌دلیل وجود **Garbage Collector**، نیاز به مخرب‌های سنتی کمتر است، اما همچنان در برخی سناریوها مرتبط است.

---

## وراثت (Inheritance)

### تعریف و مفهوم

وراثت به یک کلاس (کلاس فرزند) اجازه می‌دهد از اعضای یک کلاس دیگر (کلاس پایه یا والد) **ارث‌بری** کند. این مکانیزم امکان **کد مجدد (reusability)** و **توسعه‌پذیری (extensibility)** را فراهم می‌کند.

### نحوه پیاده‌سازی در C#

در C#، وراثت با استفاده از علامت `:` پس از نام کلاس فرزند انجام می‌شود:

```csharp
public class Animal
{
    public void Eat() => Console.WriteLine("Eating...");
}

public class Dog : Animal
{
    public void Bark() => Console.WriteLine("Barking...");
}
```

> ⚠️ در C# **وراثت چندگانه (Multiple Inheritance)** پشتیبانی نمی‌شود، اما با استفاده از **اینترفیس‌ها** می‌توان رفتارهای چندگانه را پیاده‌سازی کرد.

### کلیدواژه‌های مهم: `base`, `virtual`, `override`, `sealed`

- **`base`**: برای فراخوانی سازنده یا متدهای کلاس پایه در کلاس فرزند استفاده می‌شود.
- **`virtual`**: یک متد در کلاس پایه را قابل بازنویسی در کلاس‌های فرزند می‌کند.
- **`override`**: در کلاس فرزند، متدی را که در کلاس پایه `virtual` تعریف شده، بازنویسی می‌کند.
- **`sealed`**: از ارث‌بری بیشتر یک کلاس یا یک متد جلوگیری می‌کند.

مثال:

```csharp
public class Animal
{
    public virtual void Speak() => Console.WriteLine("Animal sound");
}

public class Cat : Animal
{
    public override void Speak() => Console.WriteLine("Meow!");
}
```

### وراثت و اصل جایگزینی لیسکوف (LSP)

**اصل LSP** (Liskov Substitution Principle) می‌گوید:  
> «اگر S زیرمجموعه‌ی T باشد، آنگاه اشیای از نوع T باید با اشیای از نوع S بدون رفتار غیرمنتظره جایگزین شوند.»

به عبارت ساده‌تر، یک کلاس فرزند **نباید** رفتار کلاس پایه را به‌صورتی تغییر دهد که کاربر کلاس پایه را گمراه کند.

---

## مخرب‌ها (Destructors)

### تعریف و کاربرد

مخرب‌ها یا **Finalizer** در C# با نام‌گذاری `~ClassName` تعریف می‌شوند و هنگامی که Garbage Collector شیء را از حافظه حذف می‌کند، به‌صورت خودکار فراخوانی می‌شوند.

```csharp
public class FileManager
{
    ~FileManager()
    {
        // آزادسازی منابع غیرمدیریت‌شده (مثل فایل‌ها یا هندل‌های سیستمی)
        Console.WriteLine("Finalizer called.");
    }
}
```

> ⚠️ مخرب‌ها **غیرقابل پیش‌بینی** هستند — زمان دقیق اجرای آن‌ها مشخص نیست.

### تفاوت با `IDisposable` و مدیریت منابع

در C#، برای مدیریت منابع (مخصوصاً منابع غیرمدیریت‌شده مانند فایل، شبکه، دیتابیس)، **الگوی `IDisposable`** ترجیح داده می‌شود:

```csharp
public class FileManager : IDisposable
{
    private bool disposed = false;

    public void Dispose()
    {
        Dispose(true);
        GC.SuppressFinalize(this);
    }

    protected virtual void Dispose(bool disposing)
    {
        if (!disposed)
        {
            if (disposing)
            {
                // آزاد کردن منابع مدیریت‌شده
            }
            // آزاد کردن منابع غیرمدیریت‌شده
            disposed = true;
        }
    }

    ~FileManager()
    {
        Dispose(false);
    }
}
```

✅ **بهترین روش**: استفاده از `using` statement همراه با `IDisposable`.

### چرخه زندگی شیء و Finalizer

- شیء ایجاد می‌شود → `Constructor` فراخوانی می‌شود.
- شیء استفاده می‌شود.
- شیء بدون مرجع می‌شود → Garbage Collector آن را علامت‌گذاری می‌کند.
- اگر Finalizer داشته باشد، در صف Finalizer قرار می‌گیرد.
- GC پس از اجرای Finalizer، شیء را از حافظه حذف می‌کند.

---

## جمع‌بندی و اصول طراحی مرتبط

- **وراثت** باید به‌منظور **توسعه‌پذیری معنادار** و نه صرفاً **کاهش کد تکراری** استفاده شود.
- رعایت **اصل LSP** در طراحی سلسله مراتب وراثت ضروری است.
- **مخرب‌ها** در C# به‌ندرت نیاز است و جای خود را به **الگوی `IDisposable`** داده‌اند.
- همیشه از `using` یا فراخوانی صریح `Dispose()` برای آزادسازی منابع استفاده کنید.

---

## منابع معتبر

1. **Microsoft Learn – Inheritance in C#**  
   🔗 https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/object-oriented/inheritance

2. **Microsoft Learn – Finalizers (Destructors)**  
   🔗 https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/classes-and-structs/destructors

3. **Microsoft Learn – Implementing a Dispose method**  
   🔗 https://learn.microsoft.com/en-us/dotnet/standard/garbage-collection/implementing-dispose

4. **"C# in Depth" – Jon Skeet** (Chapter on Object Lifetime and Finalizers)  
   📘 ISBN: 978-1617294531

5. **SOLID Principles – Liskov Substitution Principle**  
   🔗 https://learn.microsoft.com/en-us/dotnet/architecture/modern-web-apps-azure/common-web-application-architectures#liskov-substitution-principle

6. **.NET Documentation – Object-Oriented Programming**  
   🔗 https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/tutorials/oop!
