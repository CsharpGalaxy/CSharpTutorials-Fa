

1. [مقدمه: متدهای مجازی چیستند؟](#1-مقدمه-متدهای-مجازی-چیستند)  
2. [چرا از متدهای مجازی استفاده می‌شود؟](#2-چرا-از-متدهای-مجازی-استفاده-میشود)  
3. [مکانیزم فراخوانی مجازی (Virtual Method Dispatch)](#3-مکانیزم-فراخوانی-مجازی-virtual-method-dispatch)  
4. [تأثیر متدهای مجازی بر عملکرد (Performance Overhead)](#4-تأثیر-متدهای-مجازی-بر-عملکرد-performance-overhead)  
5. [نقش در اصول طراحی (Design Principles)](#5-نقش-در-اصول-طراحی-design-principles)  
6. [بهترین روش‌ها (Best Practices)](#6-بهترین-روشها-best-practices)  
7. [جمع‌بندی](#7-جمعبندی)  
8. [منابع معتبر](#8-منابع-معتبر)

---

### 1. مقدمه: متدهای مجازی چیستند؟

در C#، یک **متد مجازی** (virtual method) متدی است که در یک کلاس پایه با کلیدواژه `virtual` تعریف می‌شود و امکان **بازنویسی** (override) آن در کلاس‌های فرزند را فراهم می‌کند. این ویژگی یکی از ارکان اصلی **چندریختی** (Polymorphism) در برنامه‌نویسی شیءگرا (OOP) است.

```csharp
public class Animal
{
    public virtual void MakeSound()
    {
        Console.WriteLine("Some generic sound");
    }
}

public class Dog : Animal
{
    public override void MakeSound()
    {
        Console.WriteLine("Woof!");
    }
}
```

---

### 2. چرا از متدهای مجازی استفاده می‌شود؟

- **انعطاف‌پذیری**: امکان تغییر رفتار در زمان اجرا بر اساس نوع واقعی شیء.
- **قابلیت گسترش**: طراحی سیستم‌هایی که بدون تغییر کد اصلی قابل گسترش هستند (اصل **Open/Closed Principle**).
- **تست‌پذیری**: استفاده از مُک (mock) در تست‌های واحد برای کلاس‌هایی که از رفتار مجازی پیروی می‌کنند.

---

### 3. مکانیزم فراخوانی مجازی (Virtual Method Dispatch)

برخلاف متدهای عادی که در **زمان کامپایل** (compile-time) تعیین می‌شوند (**early binding**)، متدهای مجازی در **زمان اجرا** (runtime) بر اساس نوع واقعی شیء فراخوانی می‌شوند (**late binding**).

این فرآیند از طریق **جدول مجازی** (v-table یا virtual method table) انجام می‌شود:
- هر کلاسی که متد مجازی دارد، یک جدول داخلی نگه‌می‌دارد.
- هنگام فراخوانی، CLR (Common Language Runtime) این جدول را بررسی کرده و متد مناسب را پیدا می‌کند.

---

### 4. تأثیر متدهای مجازی بر عملکرد (Performance Overhead)

بله، متدهای مجازی **کمی هزینه عملکردی** دارند، اما در بیشتر موارد **نامحسوس** است.

#### چرا هزینه دارند؟
- نیاز به جستجوی متد در v-table.
- عدم امکان **inline کردن** متد توسط کامپایلر (Inlining).
- کاهش احتمال بهینه‌سازی توسط JIT compiler.

#### چقدر هزینه دارد؟
بر اساس آزمایش‌های مایکروسافت و جامعه توسعه‌دهندگان:
- تفاوت عملکرد در **میلیاردها فراخوانی** تنها چند میلی‌ثانیه است.
- در کاربردهای روزمره (حتی سرورهای پرترافیک) این هزینه **غیرقابل توجه** است.

> 📌 **نکته مهم**: اگر متد شما در حلقه‌های کیلر (hot loops) صدها میلیون بار فراخوانی می‌شود، ممکن است نیاز به بررسی عملکرد داشته باشید. در غیر این صورت، **خوانایی و طراحی خوب مهم‌تر از هزینه‌های ناچیز عملکردی است.**

---

### 5. نقش در اصول طراحی (Design Principles)

استفاده از متدهای مجازی مستقیماً با دو اصل مهم از **اصول SOLID** ارتباط دارد:

#### ✅ اصل Open/Closed (OCP)
> «کلاس‌ها باید باز برای گسترش، اما بسته برای تغییر باشند.»  
متدهای مجازی به شما امکان می‌دهند رفتار کلاس پایه را **بیرون از آن** گسترش دهید — بدون نیاز به تغییر کد اصلی.

#### ✅ اصل وارونگی وابستگی (DIP)
> «ماژول‌های سطح بالا نباید به ماژول‌های سطح پایین وابسته باشند. هر دو باید به یک انتزاع (abstraction) وابسته باشند.»  
متدهای مجازی این انتزاع را فراهم می‌کنند، به‌ویژه وقتی با **کلاس‌های پایه انتزاعی** یا **رابط‌ها** همراه شوند.

---

### 6. بهترین روش‌ها (Best Practices)

- ✅ فقط متدی را `virtual` کنید که **واقعاً نیاز به override شدن دارد**. (اصل YAGNI)
- ❌ از `virtual` برای همه متدها استفاده نکنید — هم برای حفظ کارایی و هم برای جلوگیری از پیچیدگی.
- ✅ در صورت نیاز به انعطاف‌پذیری بیشتر، به جای وراثت از متد مجازی، از **ترکیب (Composition)** یا **رابط‌ها (Interfaces)** استفاده کنید.
- ⚠️ مراقب **شکنندگی کلاس پایه** (Fragile Base Class Problem) باشید — تغییر در متد مجازی ممکن است رفتار تمام فرزندان را به هم بزند.

---

### 7. جمع‌بندی

- متدهای مجازی یک ابزار قدرتمند برای پیاده‌سازی **چندریختی** و رعایت **اصول طراحی** هستند.
- هزینه عملکردی آنها در اکثر موارد **ناچیز** است و نباید مانع استفاده از آنها شود.
- استفاده هوشمندانه از `virtual` به طراحی **قابل گسترش، قابل تست و انعطاف‌پذیر** کمک می‌کند.

---

### 8. منابع معتبر

1. **Microsoft Learn – Virtual Keyword**  
   [https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/virtual](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/virtual)

2. **Microsoft Learn – Polymorphism**  
   [https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/object-oriented/polymorphism](https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/object-oriented/polymorphism)

3. **CLR via C# (4th Edition) – Jeffrey Richter**  
   - فصل 10: Interfaces  
   - فصل 7: Methods and Parameters (بخش مربوط به Virtual Dispatch)

4. **Performance Guidelines – .NET Documentation**  
   [https://learn.microsoft.com/en-us/dotnet/standard/design-guidelines/performance-guidelines](https://learn.microsoft.com/en-us/dotnet/standard/design-guidelines/performance-guidelines)

5. **SOLID Principles – Martin Fowler & Robert C. Martin**  
   [https://blog.cleancoder.com/uncle-bob/2020/10/18/Solid-Relevance.html](https://blog.cleancoder.com/uncle-bob/2020/10/18/Solid-Relevance.html)

6. **BenchmarkDotNet – Real-world Performance Testing (Open Source)**  
   [https://github.com/dotnet/BenchmarkDotNet](https://github.com/dotnet/BenchmarkDotNet)  
 🚀
