
# 📘 آموزش File-scoped Namespaces در C# (از مقدماتی تا متوسط)

> 📌 **هدف**: آشنایی با ویژگی **File-scoped Namespaces** در C# 10 و بالاتر، کاربرد آن در کدنویسی مدرن و بهبود ساختار پروژه‌های شیء‌گرا (OOP)

---

## 📋 فهرست مطالب

1. [مقدمه: Namespace چیست؟](#1-مقدمه-namespace-چیست)
2. [شکل سنتی تعریف Namespace](#2-شکل-سنتی-تعریف-namespace)
3. [معرفی File-scoped Namespaces (از C# 10)](#3-معرفی-file-scoped-namespaces-از-c-10)
4. [مزایای استفاده از File-scoped Namespaces](#4-مزایای-استفاده-از-file-scoped-namespaces)
5. [محدودیت‌ها و نکات مهم](#5-محدودیت‌ها-ونکات-مهم)
6. [مقایسه کد قبل و بعد از C# 10](#6-مقایسه-کد-قبل-و-بعد-از-c-10)
7. [بهترین‌کاربردها در پروژه‌های OOP](#7-بهترینکاربردها-در-پروژههای-oop)
8. [جمع‌بندی](#8-جمعبندی)
9. [منابع معتبر](#9-منابع-معتبر)

---

## 1. مقدمه: Namespace چیست؟

در زبان C#، **namespace** (فضای نام) یک مکانیزم برای سازماندهی کلاس‌ها، رابط‌ها، ساختارها و سایر انواع است. این کار به جلوگیری از **تداخل نام‌ها** (name collision) کمک می‌کند و کد را خوانا‌تر و مدیریت‌پذیرتر می‌سازد.

به عنوان مثال، شما ممکن است دو کلاس با نام `Logger` داشته باشید — یکی برای لاگ‌گیری سیستم و دیگری برای لاگ‌گیری کاربران. با استفاده از namespace می‌توانید آن‌ها را جدا کنید:

```csharp
namespace MyApp.SystemTools;
class Logger { }

namespace MyApp.UserTools;
class Logger { }
```

---

## 2. شکل سنتی تعریف Namespace

قبل از C# 10، namespaceها به صورت بلوکی تعریف می‌شدند:

```csharp
namespace MyApp.Utilities
{
    public class StringUtils
    {
        public static string Reverse(string input) => new string(input.Reverse().ToArray());
    }
}
```

این سبک کاملاً معتبر است، اما در فایل‌هایی که فقط یک namespace دارند، می‌تواند حجم کد را افزایش دهد و خوانایی را کاهش دهد، به خصوص وقتی همهٔ محتوای فایل در یک namespace قرار دارد.

---

## 3. معرفی File-scoped Namespaces (از C# 10)

در **C# 10** (انتشار یافته در نوامبر 2021 به همراه .NET 6)، مایکروسافت ویژگی جدیدی را معرفی کرد: **File-scoped Namespaces**.

این ویژگی به شما اجازه می‌دهد به جای استفاده از بلوک `{ }` برای namespace، آن را در یک خط با پایان‌دهنده `;` تعریف کنید:

```csharp
namespace MyApp.Utilities;

public class StringUtils
{
    public static string Reverse(string input) => new string(input.Reverse().ToArray());
}
```

> 🔹 **نکته**: تمام محتوای فایل (کلاس‌ها، اینترفیس‌ها، انوام‌ها و...) در همان namespace تعریف‌شده قرار می‌گیرند.

---

## 4. مزایای استفاده از File-scoped Namespaces

| مزیت | توضیح |
|------|--------|
| ✅ کاهش indentation (تو رفتگی) | دیگر نیازی به یک سطح اضافه‌ی تو رفتگی نیست — کد تمیزتر می‌شود. |
| ✅ خوانایی بالاتر | محتوای اصلی فایل (کلاس‌ها) از ابتدای خط شروع می‌شوند. |
| ✅ انطباق با سبک مدرن کدنویسی | مشابه سبک‌هایی که در زبان‌هایی مثل Python یا JavaScript دیده می‌شود. |
| ✅ کاهش خطوط کد | فایل‌های کوچک‌تر و مختصرتر. |

---

## 5. محدودیت‌ها و نکات مهم

- ❌ **فقط یک namespace در هر فایل** می‌تواند به صورت file-scoped تعریف شود.
- ❌ نمی‌توان از هر دو سبک (بلوکی و file-scoped) در یک فایل استفاده کرد.
- ❌ namespaceهای تو در تو (nested namespaces) را نمی‌توان با این سبک نوشت.
  
مثال غیرمجاز:
```csharp
// ❌ خطای کامپایل
namespace Outer;
namespace Inner; // این مجاز نیست
```

اگر نیاز به namespaceهای تو در تو دارید، باید به سبک سنتی برگردید:

```csharp
namespace Outer.Inner
{
    class MyClass { }
}
```

---

## 6. مقایسه کد قبل و بعد از C# 10

### قبل از C# 10 (سبک سنتی)

```csharp
namespace MyApp.Services
{
    public class EmailService
    {
        public void Send(string to, string message) { }
    }
}
```

### از C# 10 به بعد (File-scoped)

```csharp
namespace MyApp.Services;

public class EmailService
{
    public void Send(string to, string message) { }
}
```

تفاوت ظاهری کوچک است، اما در پروژه‌های بزرگ، این تغییر می‌تواند **هزاران خط کد** را ساده‌تر کند.

---

## 7. بهترین‌کاربردها در پروژه‌های OOP

در ریپوزیتوری‌های آموزشی OOP، استفاده از file-scoped namespaces می‌تواند:

- ✨ **تمرکز را روی اصول OOP** (مانند انسجام، کپسوله‌سازی، و...) بیشتر کند، نه ساختار فایل.
- 📁 **ساختار پروژه را ساده‌تر** نشان دهد — به خصوص برای دانش‌آموزان مبتدی.
- 🚀 **آموزش مفاهیم مدرن C#** را امکان‌پذیر کند، بدون اینکه دانش‌آموز با کدهای قدیمی گیج شود.

> 💡 **پیشنهاد**: در ریپوزیتوری آموزشی خود، از C# 10 یا بالاتر استفاده کنید و file-scoped namespaces را به عنوان استاندارد در نظر بگیرید — مگر اینکه نیاز به namespaceهای تو در تو داشته باشید.

---

## 8. جمع‌بندی

- **File-scoped namespaces** یک ویژگی مدرن در C# 10 است.
- سبک جدید، کد را **کوتاه‌تر، تمیزتر و خواناتر** می‌کند.
- برای بیشتر پروژه‌های OOP — به خصوص آموزشی — گزینه‌ی عالی‌ای است.
- در صورت نیاز به namespaceهای پیچیده، همچنان می‌توان از سبک سنتی استفاده کرد.

---

## 9. منابع معتبر

1. **مستندات رسمی مایکروسافت (Microsoft Learn)**  
   🔗 [File-scoped namespaces – C# 10](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/proposals/csharp-10.0/file-scoped-namespaces)  
   🔗 [Namespaces – C# Guide](https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/types/namespaces)

2. **C# Language GitHub Proposal (by dotnet team)**  
   🔗 [GitHub: File-scoped namespaces proposal](https://github.com/dotnet/csharplang/blob/main/proposals/csharp-10.0/file-scoped-namespaces.md)

3. **C# 10 and .NET 6 – Mark J. Price (کتاب آموزشی معتبر)**  
   ISBN: 978-1801077361 – فصل 3: Modern C# Syntax

4. **.NET Blog – Official Announcement**  
   🔗 [Welcome to C# 10!](https://devblogs.microsoft.com/dotnet/welcome-to-c-10/)
🎓
