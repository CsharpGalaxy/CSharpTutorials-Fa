
## فهرست مطالب

1. [مقدمه](#مقدمه)  
2. [تعریف Init-only Setters](#تعریف-init-only-setters)  
3. [چرا Init-only Setters معرفی شدند؟](#چرا-init-only-setters-معرفی-شدند)  
4. [نحوه استفاده در کد](#نحوه-استفاده-در-کد)  
5. [تفاوت با خصوصیات فقط خواندنی (Readonly Properties)](#تفاوت-با-خصوصیات-فقط-خواندنی-readonly-properties)  
6. [init در ترکیب با رکوردها (Records)](#init-در-ترکیب-با-رکوردها-records)  
7. [Init-only Setters و Immutable Objects](#init-only-setters-و-immutable-objects)  
8. [محدودیت‌ها و نکات مهم](#محدودیت‌ها-و-نکات-مهم)  
9. [بهترین روش‌های استفاده (Best Practices)](#بهترین-روش‌های-استفاده-best-practices)  
10. [جمع‌بندی](#جمع‌بندی)  
11. [منابع معتبر](#منابع-معتبر)

---

## مقدمه

در جهان برنامه‌نویسی مدرن، **شیء‌گرایی ایمن و مقیاس‌پذیر** یکی از اهداف اصلی است. زبان C# در سال‌های اخیر با ارائه ویژگی‌هایی همچون **Records**، **Pattern Matching** و **Init-only Setters** گام‌های بزرگی در جهت پشتیبانی از **Immutable Objects** و **Functional Programming Paradigms** برداشته است.

در این مقاله، به‌طور کامل و مستند، مفهوم **Init-only Setters** را بررسی می‌کنیم — از تعریف ابتدایی تا کاربردهای پیشرفته آن در توسعه نرم‌افزارهای مدرن.

---

## تعریف Init-only Setters

**Init-only Setters** یک ویژگی جدید در C# 9.0 (انتشار یافته در نوامبر 2020) هستند که به شما اجازه می‌دهند یک خاصیت (Property) را **فقط در زمان مقداردهی اولیه (initialization)** تنظیم کنید و پس از آن، دیگر امکان تغییر آن وجود نداشته باشد.

این ویژگی با کلمه کلیدی `init` جایگزین `set` در خصوصیات می‌شود:

```csharp
public class Person
{
    public string FirstName { get; init; }
    public string LastName { get; init; }
}
```

---

## چرا Init-only Setters معرفی شدند؟

قبل از C# 9، برای ساخت اشیاء **غیرقابل تغییر (Immutable)**، چند راه وجود داشت:

1. استفاده از **فیلدهای `readonly`** و مقداردهی آن‌ها فقط در سازنده.
2. استفاده از **Properties فقط خواندنی** و مقداردهی در سازنده.

اما این روش‌ها **مقداردهی شیء با Initializer Syntax** (`new Person { FirstName = "Ali" }`) را غیرممکن می‌کردند.

**Init-only setters** این امکان را فراهم کردند که همزمان از:
- **Immutable شدن داده پس از ایجاد شیء**، و
- **سینتکس راحت و خوانا برای مقداردهی اولیه**

استفاده کنید.

---

## نحوه استفاده در کد

### ✅ مثال مجاز:
```csharp
var person = new Person
{
    FirstName = "Reza",
    LastName = "Ahmadi"
};
// OK: مقداردهی در هنگام ایجاد شیء
```

### ❌ مثال غیرمجاز:
```csharp
person.FirstName = "Ali"; // ❌ خطای کامپایل!
// CS8852: Init-only property or indexer 'Person.FirstName' can only be assigned in an object initializer
```

این خطا در **زمان کامپایل** رخ می‌دهد — یعنی امنیت نوع (Type Safety) و جلوگیری از اشتباهات را تضمین می‌کند.

---

## تفاوت با خصوصیات فقط خواندنی (Readonly Properties)

| ویژگی | Init-only Setter | فقط خواندنی (Readonly) |
|--------|------------------|------------------------|
| مقداردهی در سازنده | ✅ | ✅ |
| مقداردهی با Object Initializer | ✅ | ❌ |
| تغییر پس از ایجاد شیء | ❌ | ❌ |
| نیاز به سازنده | ❌ (اختیاری) | ✅ (معمولاً) |

مثال با فقط خواندنی:
```csharp
public class Person
{
    public string FirstName { get; }
    public Person(string firstName) => FirstName = firstName;
}
// فقط با سازنده قابل مقداردهی
```

اما با `init`:
```csharp
public class Person
{
    public string FirstName { get; init; }
}
// بدون نیاز به سازنده!
```

---

## init در ترکیب با رکوردها (Records)

در C# 9 و بالاتر، **Records** به‌صورت پیش‌فرض از `init` استفاده می‌کنند:

```csharp
public record Person(string FirstName, string LastName);
```

این معادل است با:
```csharp
public record Person
{
    public string FirstName { get; init; }
    public string LastName { get; init; }
}
```

همچنین، قابلیت **With-Expressions** را فعال می‌کند:
```csharp
var person1 = new Person("Ali", "Rahimi");
var person2 = person1 with { FirstName = "Reza" }; // ایجاد نسخه جدید با تغییر جزئی
```

> 🔹 این ترکیب، **Immutable Data Modeling** را در C# بسیار قدرتمند کرده است.

---

## Init-only Setters و Immutable Objects

**Objective**: ساخت اشیاء‌ای که پس از ایجاد، **تغییرناپذیر** باشند.

- کاهش باگ‌های ناشی از تغییر وضعیت غیرمنتظره
- امنیت بیشتر در برنامه‌نویسی چندنخی (Multithreading)
- تطابق بهتر با اصول Functional Programming

مثال کاربردی در Domain-Driven Design (DDD):
```csharp
public record OrderItem
{
    public Guid ProductId { get; init; }
    public int Quantity { get; init; }
    public decimal UnitPrice { get; init; }
}
```

این ساختار تضمین می‌کند که یک آیتم سفارش پس از ایجاد، دیگر قابل اصلاح نیست — مگر اینکه یک نسخه جدید از آن ساخته شود.

---

## محدودیت‌ها و نکات مهم

1. **فقط در Object Initializer قابل استفاده است**  
   نمی‌توان از `init` در متدها یا بعد از ساخت شیء استفاده کرد.

2. **در وراثت کار می‌کند، اما با احتیاط**  
   اگر کلاس پایه یک خاصیت `init` داشته باشد، کلاس فرزند نمی‌تواند آن را override کند مگر اینکه خودش هم `init` باشد.

3. **با رابط‌ها (Interfaces) سازگار است**  
   ```csharp
   public interface IPerson
   {
       string Name { get; init; }
   }
   ```

4. **در زمان اجرا قابل دور زدن نیست**  
   برخلاف برخی زبان‌ها، C# این را در سطح **کامپایلر** اعمال می‌کند — نه فقط در سطح داکیومنت.

---

## بهترین روش‌های استفاده (Best Practices)

✅ **استفاده در مدل‌های داده (Data Transfer Objects)**  
✅ **ترکیب با Records برای ساختارهای داده‌ای مقیاس‌پذیر**  
✅ **استفاده در Domain Models در معماری‌های DDD**  
❌ **استفاده در کلاس‌هایی که نیاز به تغییر وضعیت دارند (مثل ViewModel در WPF/MAUI)**  
❌ **اجبار به استفاده از init در تمام خصوصیات بدون دلیل**

---

## جمع‌بندی

- `init` یک **کلمه کلیدی ایمن‌ساز** است که امکان مقداردهی اولیه را فراهم می‌کند، اما تغییر پس از آن را ممنوع می‌کند.
- بخشی از تلاش مایکروسافت برای پشتیبانی از **Immutable Programming** در C#.
- همراه با **Records** و **With-Expressions**، انقلابی در ساخت مدل‌های داده ایمن ایجاد کرده است.
- برای توسعه‌دهندگان OOP، این ویژگی تعادل خوبی بین **قابلیت خوانایی کد** و **ایمنی داده** ایجاد می‌کند.

---

## منابع معتبر

1. **مستندات رسمی مایکروسافت (Microsoft Docs)**  
   [Init-only Setters - C# 9.0](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/proposals/csharp-9.0/init)  
   [Records in C#](https://learn.microsoft.com/en-us/dotnet/csharp/whats-new/tutorials/records)

2. **C# Language Specification (GitHub - dotnet/csharplang)**  
   [Proposal: Init-only members](https://github.com/dotnet/csharplang/blob/main/proposals/csharp-9.0/init.md)

3. **کتاب "C# 10 and .NET 6" – Mark J. Price**  
   فصل 8: Building reusable types with object-oriented programming

4. **.NET Blog – مایکروسافت**  
   [Welcome to C# 9.0](https://devblogs.microsoft.com/dotnet/welcome-to-c-9-0/)

5. **Pluralsight / Microsoft Learn**  
   دوره‌های رسمی درباره Modern C# Features

!
